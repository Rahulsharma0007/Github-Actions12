name: Hello workflow

on:
  push: 
    branches: [main]

jobs:
   greet:
     runs-on: ubuntu-latest
     steps:
       - name: "hello Dosto"
         run: echo "hello dosto"

       - name: "say hello to user"
         run: echo "hello $USER"
